# Core Flask dependencies
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# Database
# SQLite is included with Python, no additional package needed

# System monitoring (for settings API)
psutil==5.9.6

# Development tools (optional)
python-dotenv==1.0.0

# Testing (optional)
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# Logging
colorlog==6.8.0

# File watching (for future real-time updates)
watchdog==3.0.0